type Props = {
  setItemsPerPage: React.Dispatch<React.SetStateAction<number>>;
  setPageNumber: React.Dispatch<React.SetStateAction<number>>;
};

export const Pagination = ({ setItemsPerPage, setPageNumber }: Props) => {
  return <div>pagination </div>;
};
